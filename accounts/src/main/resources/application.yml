server:
  port: 8092
spring:
  application:
    name: "accounts"
  datasource:
    url: jdbc:mysql://localhost:3306/accounts_bank_aht
    username: root
    password: hung1218
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  sql:
    init:
      mode: always
